# Core Dependencies
numpy>=1.24.0  # NumPy 2.x is now supported by PyTorch and TensorFlow
opencv-python>=4.8.0
scipy>=1.11.0

# MediaPipe for gaze tracking and face mesh
mediapipe>=0.10.0

# TensorFlow Lite for emotion detection
tensorflow>=2.13.0
# Alternative: tflite-runtime for edge devices (smaller footprint)
# tflite-runtime>=2.13.0

# Deep learning for facial emotion recognition
keras>=2.13.0
fer>=22.5.0  # Facial Expression Recognition library with pre-trained models
transformers>=4.30.0  # Hugging Face transformers for ViT model
torch>=2.0.0  # PyTorch for ViT model
Pillow>=9.0.0  # Image processing for transformers

# Mouse tracking for CLI calculation
pynput>=1.7.6

# Data processing and analysis
pandas>=2.0.0
scikit-learn>=1.3.0

# Statistical analysis
statsmodels>=0.14.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Configuration management
pyyaml>=6.0

# WebSocket server for browser extension
websockets>=12.0

# Logging and utilities
python-dateutil>=2.8.0

# PyGaze for professional eye tracking
# Note: PyGaze requires PsychoPy, which may need system dependencies
# On macOS: brew install hdf5 (for PsychoPy's tables dependency)
# Or use: conda install -c conda-forge psychopy
python-pygaze>=0.6.0
# psychopy>=2023.0.0  # Uncomment if installing manually (may require HDF5)

# Pygame for gaze-contingent displays (optional, for examples)
pygame>=2.5.0

# Testing (optional)
pytest>=7.4.0
pytest-cov>=4.1.0

# Code quality (optional)
black>=23.7.0
flake8>=6.1.0
mypy>=1.5.0

